function sketchTwo() {
    // global variables
  function title() {}

  function interface() {
    // interface div 
    function grid() {
        // grid div
      function createGrid() {
        // grid creation variables
        function createGridContainer() {
            // gridContainer variables
          const items = () => {};
          const gridEventListeners = () => {};
        }

        function deleteGrid() {}
      }
    }

    function controls() {
      // Select the grid and grid columns
      // create control div - child of interface
        // create control list - child of control div

      function colors() {
        // color variables
        // child of control list
         const colorArray = [
           "Red",
           "Orange",
           "Yellow",
           "Green",
           "Blue",
           "Indigo",
           "Violet",
         ];
        const colorChoice = () => {
            // switch case for each color in the Color array
                // toggles the color evaluated as the class to set the grid divs.
        };
        const setGridColor = () => {};
        // set color toggle
      }

      function gridSize() {
        // grid size variables
        // child of control list
        const gridChoice = () => {};
        const newGridSize = () => {
            // delete grid
        };
    }

      function reset() {
        //reset variables
        // child of control list
        const resetChoice = () => {};
        const resetGrid = () => {};
      }
    }
  }
}
